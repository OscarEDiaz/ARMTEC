export const parseInstructions = (instructions) => {
    instructions.forEach(instruction => {
        switch (instruction.type) {
            case 'forward':
                
                break;
        
            default:
                break;
        }
    });
}